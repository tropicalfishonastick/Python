# Import the 'statistics' module to perform statistical calculations
import statistics

# Calculate the mean (average) of a list of numbers using the 'mean()' function from the 'statistics' module
print(statistics.mean([100, 90]))
