# Prompt the user for input and store it as integers x and y
x = int(input("What's x? "))
y = int(input("What's y? "))

# Check if x is less than y
if x < y:
    # If x is indeed less than y, print this message
    print("x is less than y")